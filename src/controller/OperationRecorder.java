package controller;

import java.util.ArrayList;

public class OperationRecorder {

    /**
     * @param expression
     * @param operator
     * @param index
     */
    public static void OperationRecordReader(ArrayList<String> expression, String operator, Integer index) {
//            System.out.println("operator: "+operator + " => " + expression.get(index) + " " + operator + " " +
//                    expression.get(index+1) + " = " + (Double.parseDouble(expression.get(index))
//                    + Double.parseDouble(expression.get(index+1))));
    }
}
